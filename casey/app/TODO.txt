See ROADMAP.txt for upcoming features.

* Create a grey icon for "track" with the info "i" label.
* Make everything faster.. API calls are slow. XML parsing is slow. Optimize. Optimize Optimize!
* Status indicator during login, and other long fetches.
* Fetch all profile info on login in one http request.
* Hitting return in the password field should click the login button
* Something should be displayed in the Recent Stations list when there are no recent stations
* Search-as-you-type in the New Station activity
* "Radio Loading" indicator in the player when stuff is loading, instead of blank fields
* Player controls should be bigger?
* The UI should be more responsive when http/xml things are happening in the background
